# Archive — Superseded Docs

These docs were the initial design iteration. They describe a **Python/discord.py/VPS/SQLite/APScheduler** architecture that was replaced when the project moved to **TypeScript/Vercel/Supabase/Inngest**.

The bot behavior specs (what each module posts, filtering logic, embed formats) were carried forward into the current docs. The tech stack, deployment, Discord integration model, and config/state management described here are **no longer accurate**.

**Current docs are in the parent directory (`docs/`).**
